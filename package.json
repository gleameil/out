{
  "name": "in",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel -p 8080",
    "build": "parcel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel": "^2.15.4",
    "sharp": "^0.33.5",
    "typescript": "^5.8.3"
  }
}
